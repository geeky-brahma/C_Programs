#include <stdio.h>
int main()
{
    int n;
    printf("Enter the number of digits: ");
    scanf("%d", &n);

    fib(n-2);
    return 0;
}

int fib(int n)
{
    int sum;
    if (n==0)
    {
        return 
    }
}